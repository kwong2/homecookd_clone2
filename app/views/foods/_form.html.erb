<%= form_with(model: food, local: true) do |form| %>
  <% if food.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(food.errors.count, "error") %> prohibited this food from being saved:</h2>

      <ul>
      <% food.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :food_name %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price, id: :food_price %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :food_description %>
  </div>

<!--   <div class="field">
    <%= form.label :picture %>
    <%= form.text_field :picture, id: :food_picture %>
  </div> -->


  <%= form.submit "Post", class: "btn btn-primary" %>
  <span class="picture">
    <%= form.file_field :picture, accept: 'image/jpeg,image/gif,image/png' %>
  </span>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :food_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
